```plain
> git config --global user.email "UserEmail"
> git config --global user.name "UserName"
> git clone https://github.com/Rundll86/SwordEngineRuntime.git
> cd SwordEngineRuntime
```
解压electron到 `source\Core`
```plain
> pip install pyinstaller
> pip install easygui
> pip install conkits
> build-run.bat
> build-proj.bat
> build-installer.bat
> clean.bat
> start dist\installer.exe
```